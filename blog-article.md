# 从重复劳动到 AI Skill：我是如何把一个 GitHub 上传流程做成一个产品的

> 本文记录了一次从"我为什么每次都要重复这些步骤"到"做一个让任何人都能用的 Skill"的完整思考过程。  
> 不是炫技，是复盘。

---

## 起点：一个让人烦躁的重复动作

我有个习惯：每完成一个项目，都会把它上传到 GitHub 存档。

听起来很简单。但每次实际操作时，我总会在某个地方卡住：

- LICENSE 里的 `[Author]` 忘了替换，上传完才发现
- `curl` 创建仓库时，描述里带了中文，报 `Problems parsing JSON`，查了半天才找到原因
- `git remote add origin` 填错了仓库地址，差点把新代码推到了别人的旧项目上
- 每次都要重新回忆"下一步是什么"

这些问题单独看都很小，但加在一起，足以把一个本应顺畅的流程变成一段让人烦躁的经历。

我意识到：**我在用脑力处理一件本不需要脑力的事。**

---

## 第一次尝试：写个文档

最直觉的反应是"写个 SOP"。我把完整步骤写成了一个 Markdown 文件，放在桌面备用。

用了两次之后，问题出现了：

1. 文档是静态的，它只能"告诉我做什么"，但它不能"检查我有没有做对"
2. 我依然可能跳过某一步（比如敏感文件扫描），文档不会拦截我
3. 遇到新的报错，文档不会更新，只有我的记忆会更新

**静态文档解决了"记忆"问题，但没有解决"执行"问题。**

---

## 转折点：AI 介入后的不同

某次我在用 Claude Code 协作完成一个项目，项目结束后想上传 GitHub，就顺手把上传步骤描述给了 AI，让它帮我执行。

那一次，有些事情发生了变化：

- AI 在执行阶段三（创建仓库）前，自动检查了我的 LICENSE 文件，发现 `[Author]` 还没替换，主动提醒我——这件事以前总靠运气
- 当 `git push` 因为网络问题失败时，它没有只报一行错误让我自己查，而是解释了原因，给出了重试命令
- 它记住了整个过程的上下文，每一步结束后都告诉我"下一步是什么，为什么"

我当时想的是：**这不是在"执行脚本"，这更像是"和一个有经验的同事结对操作"。**

这个感觉很关键。

---

## 产品化的关键问题：为什么不直接用 `gh`？

在决定把这个流程做成一个独立产品之前，我问了自己一个问题：

> **`gh` CLI 不是已经能做这件事了吗？**

我仔细想了一下，两者的本质不同：

`gh` 是一个**工具**：它提供原子命令，你需要自己组合，自己排错，自己记住流程顺序。

我想做的是一个**流程**：它知道整件事的上下文，在关键节点主动预检，在出错时给出针对性的处理方案，而不是把报错文本丢给你。

就像 GPS 导航和一张地图的区别：地图包含了所有信息，但 GPS 知道你在哪里、下一步该做什么、前方是否有拥堵。

**工具给你能力，流程给你确定性。**

这是我决定继续做这件事的核心理由。

---

## 设计阶段：什么叫"智能"？

我不想做一个"聪明地执行命令"的 Skill，我想做一个"在正确的时机提出正确的问题"的 Skill。

这个区别体现在两个具体设计上：

### 设计一：智能预检而不是事后报错

传统流程是：你执行完所有步骤，上传完成，然后发现 LICENSE 里还是 `[Author]`。

我的设计是：**在阶段一，主动运行检测命令，发现问题立即停止**。

```bash
grep -n "\[Author\]" LICENSE
# 有输出 → 阻断流程，提示修复
# 无输出 → ✅ 通过，继续
```

这一条来自真实的踩坑：我有一次把含有 `[Author]` 的 LICENSE 推到了 GitHub，两天后才发现。

**预检的意义不是麻烦，是把"事后发现"变成"事前阻断"。**

### 设计二：失败自愈而不是报错中止

`git push` 失败的原因有很多：网络超时、代理拦截、Token 过期、远程冲突……

传统工具的处理是：打印一行报错，流程结束，剩下的是你的事。

我的设计是：把失败类型分类，每种类型对应一个具体的处理方案，包括可以直接复制运行的重试命令：

```bash
# 网络偶发失败时的自动重试
for i in 1 2 3; do
  git push -u origin master && break || (echo "第 $i 次失败，30秒后重试..."; sleep 30)
done
```

这不是魔法，是把"以前只存在我脑子里的处理经验"变成了显式的文档。

---

## 踩坑记录：那些让人崩溃的细节

做这个项目的过程中，我记录了所有真实遇到的问题。以下几个最具代表性：

**坑一：curl 描述含中文 → `Problems parsing JSON`**

我第一次用 API 创建仓库时，description 里写了中文，得到了这个错误，查了将近半小时。

解决方案很简单：description 只用英文，JSON 用单引号包裹而不是双引号加转义。但如果没人告诉你，你很难第一时间想到。

**坑二：`git remote add` 填错仓库地址**

我曾经把 `E:\AI-Methodology` 的内容差点推到了 `seed-ai`（一个已有内容的旧仓库）。

这让我在 Skill 的第四阶段加了一个强制步骤：`git remote -v` 验证地址，在确认输出正确之前不执行 push。

**坑三：LICENSE `[Author]` 占位符**

从 MIT License 模板复制来的 `[Author]`，太容易被遗忘了。它不影响代码运行，但它影响法律效力。

这个坑变成了智能预检的第一条规则。

---

## 产品定位：它是什么，不是什么

在写 README 时，我意识到必须说清楚一件事：

**它不是 `gh` 的替代品，也不是一个自动化脚本，它是一个交互式的流程向导。**

它的价值不在于速度，而在于**确定性**——每次执行完，你可以确信：
- 没有敏感文件被上传
- LICENSE 有法律效力
- 仓库地址没有填错
- 网络问题有处理方案

这种确定性，是纯命令行工具很难提供的。

---

## 工程化：从个人工具到可复用产品

把一个"自己用的流程"变成"别人也能用的产品"，需要做几件具体的事：

**第一：去掉所有个人专属内容**

原始版本里有我自己的用户名、仓库地址、具体路径。全部替换为 `[用户名]`、`[仓库名]`、`[项目路径]` 的通用占位符。

**第二：把所有"我知道的"写成文档**

踩坑经验不能只存在我脑子里。每个坑：现象是什么、原因是什么、修复方法是什么，写成排查表。

**第三：双语文档**

中文用户和英文用户都需要能读懂。不是机器翻译，是针对不同受众重新组织的内容。

**第四：项目矩阵**

这个 Skill 是从 [AI-Project-OS](https://github.com/jiayu6954-sudo/AI-Project-OS)（一套完整的 AI 协作工程方法论）中提炼出来的子项目。  
两个项目互相链接，形成项目矩阵，而不是孤立的仓库。

---

## 更大的背景：为什么是 Claude Code Skill？

这个 Skill 的载体选择不是随意的。

Claude Code Skill 是一种特殊的知识载体：它是 Markdown 文件，可以被 AI 读取并作为工作流触发，但它本身也是人类可读的文档。

这意味着：
- 你可以用 `/github-upload` 触发完整流程
- 你也可以直接阅读 `github-upload.md`，手动执行每一步
- 你还可以把内容复制给 ChatGPT、Gemini，同样有效

**它不绑定任何特定工具，但在 Claude Code 里用起来体验最好。**

这是我认为 AI Skill 这个形态最有意思的地方：知识和流程被显式记录下来，而不是藏在某个工具的内部逻辑里。

---

## 反思：产品思维 vs 工程思维

做这件事让我想清楚了一个区别：

**工程思维**问的是：*怎么实现它？*  
**产品思维**问的是：*它为谁解决了什么问题，用户在哪个环节会卡住？*

如果只有工程思维，我会写一个 shell 脚本，把所有步骤串起来，执行一个命令完事。

但正是产品思维让我想到：用户最容易在哪里出错？（LICENSE 占位符、curl 中文描述、remote 地址填错）。用户出错后最需要什么？（不是报错代码，是下一步该怎么做）。

**好的工具解决问题，好的产品预见问题。**

---

## 如果你也有类似的重复劳动

这篇文章的终点不是推销这个 Skill，而是分享一种思路：

> 当你发现自己在重复做同一件事时，  
> 先问：这件事的哪些步骤依赖经验？哪些步骤容易出错？  
> 然后问：如果把这些经验显式化、流程化，它会变成什么？

对我来说，答案是这个 Skill。  
对你来说，答案可能完全不同——但思路是一样的。

---

## 项目地址

- **github-upload-skill**：https://github.com/jiayu6954-sudo/github-upload-skill  
  一键上传 GitHub 的完整流程 Skill，含智能预检 + 失败自愈

- **AI-Project-OS**：https://github.com/jiayu6954-sudo/AI-Project-OS  
  工业级 AI 协作工程方法论，包含 6 阶段工作流、代码审查清单、案例知识库

---

*如果你也有"把重复劳动变成可复用产品"的经历，欢迎在评论区聊聊。*  
*如果这篇文章对你有用，给仓库一个 ⭐ 是最好的反馈。*
